import React from 'react';
import './loading-page.css';

const LoadingPage = () => {
    return (
        <section className={''}>
            <div className={'loading loading05'}>
                <span className={'title-gradiant'}>R</span>
                <span className={'title-gradiant'}>R</span>
            </div>
        </section>
    );
};

export default LoadingPage;
